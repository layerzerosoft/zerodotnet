# Claude Instructions

Use `AGENTS.md` as the canonical ZeroDotNet rulebook.

Do not duplicate or reinterpret the rules here. If instructions diverge, update
`AGENTS.md` first, then keep this file as a pointer.
